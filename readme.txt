1. Start the project by running npm init
    - npm init
2. Install TypeScript, Jest, and the TypeScript compiler
    - npm install typescript jest ts-node --save-dev 
3. Run an execute for the test
    - npm test